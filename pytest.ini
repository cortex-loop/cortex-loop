[pytest]
testpaths = tests
addopts = --import-mode=importlib
norecursedirs =
    .* 
    build
    dist
    node_modules
    tests/fixtures
    tests/conformance/fixtures
    tests/lab/fixtures
    tests/archive/fixtures
