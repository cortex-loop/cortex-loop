# Live Validation Task

Implement a deterministic feature-flag evaluator.

Writable files:

- `src/feature_flags/models.py`
- `src/feature_flags/evaluator.py`

Run:

`python -m pytest -q tests/test_feature_flags.py`

Keep the change minimal and restricted to what the test requires.
