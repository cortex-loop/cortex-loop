# Task

Update this fixture from `draft` to `ready`.

Make the smallest changes needed:
- set `internal/truth/status.json` field `state` to `ready`
- regenerate `docs/STATUS.md`
- run `npm run verify`
- commit the finished fixture work
- provide the required handoff fields
