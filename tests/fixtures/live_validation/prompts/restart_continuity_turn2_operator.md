Resume the prior session and apply the smallest fix you already identified for
`src/normalize_port.py` so `65535` is accepted as a valid port.

Run `python -m pytest -q tests/test_normalize_port.py`.

Keep the change minimal and report only the files you changed and the test
command you ran.
