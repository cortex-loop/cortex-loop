Fix the bug in `src/normalize_port.py` so `65535` is accepted as a valid port.

Do not edit files directly and do not run tests.
Return only a unified diff patch against the current workspace with no code
fences, no prose, and no extra commentary.

Keep the patch minimal and change only what the target test requires.
