# CORTEX_V2_CLAUDE_RUNTIME_RESTACK_PROGRAM_0

Date: 2026-03-27
Status: active planning brief for the A1 Claude runtime/product parity train

## Purpose

This document opens the next explicit post-accepted-`G1` train:

- land the Claude documented host-event runtime shell, raw-transcript ingress shell, loopback service shell, and bounded host-control lane in dependency order,
- reuse accepted K3 executive allocation diagnostics and accepted G1 runtime/product parity patterns exactly where they remain lawful,
- and restore Claude/Anthropic to the active post-closeout host roadmap before a later multi-host live validation train.

This brief does not override:

- `docs/CORTEX_V2_CORE_2.md`
- `docs/CORTEX_V2_SRE_2.md`
- `docs/CORTEX_V2_AUX_2.md`
- `docs/CORTEX_V2_IMPLEMENTATION_MASTER_PLAN_2.md`
- `docs/CORTEX_V2_PHASE_GATES_2.md`
- `docs/CORTEX_V2_MATH_TO_CODE_CORRESPONDENCE.md`

## Accepted parent and rationale

Accepted parent for this train:

- branch: `codex/g1-gemini-runtime-product-parity`
- commit: `9dfe38a`

Why this train opens now:

- the archive implementation plan treated Claude as the first host-shaped reference lane and that host is still part of the intended product space,
- the active v2 mainline currently has no Claude runtime/product shell even though Gemini and OpenAI now do,
- and the smallest honest correction is one bounded Claude/Anthropic parity train before later multi-host live validation.

## Locked scope

This train remains:

- runtime/product only
- Claude/Anthropic only
- dependency-ordered
- packet-subordinate
- and reuse-first rather than new executive doctrine

This train does **not** authorize:

- support-memory runtime
- mediation implementation / `Q_t^{final}`
- generic runtime abstraction
- generic service abstraction
- runtime AUX activation
- offline consolidation
- multi-agent orchestration
- extra Gemini or OpenAI widening
- or multi-host live validation in the same train

## Planned seam order

1. `A1` Claude documented host-event runtime shell
2. `A2` Claude raw-transcript ingress shell
3. `A3` Claude loopback service shell
4. `A4` Claude bounded host-control lane

Each seam must end clean before the next opens.

## Default implementation constraints

Unless official Claude host docs force a narrower lawful boundary, the default Claude train should:

- preserve host-specific naming and lifecycle truth rather than flattening Claude into Gemini/OpenAI doctrine,
- reuse accepted K3 executive allocation diagnostics exactly for current scope,
- keep loopback-only service law and one active session per process,
- keep the first outbound Claude lane text-only and strict-whitelist,
- prefer stdlib HTTP transport and internal fixture mode for canonical tests,
- and keep persisted truth bounded to exact `continuity_truth` plus bounded `control_residue`.

## Current planning state before implementation

On the accepted G1 line at `9dfe38a`:

- no Claude runtime/product code is currently landed,
- no Claude runtime-program phase-gate rows are currently open,
- no Claude runtime-program briefs beyond this planning brief exist yet,
- and the next lawful implementation move is to cut a dedicated `codex/a1-claude-runtime-product-parity` branch and land the `A1` documented host-event runtime shell first.

## Explicitly blocked moves

This planning brief does not authorize:

- skipping directly to multi-host live validation before Claude parity exists,
- generic multi-host runtime/service doctrine,
- tools or tool-result submission in the first Claude outbound lane,
- mediation implementation,
- runtime AUX activation,
- offline consolidation,
- or any reinterpretation of accepted G1 truth.
