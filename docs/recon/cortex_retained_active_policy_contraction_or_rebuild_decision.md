# Cortex Retained Active-Policy Contraction Or Rebuild Decision

Surface: architecture/contraction decision

## Summary

Verdict: `decision_contract_retained_spine_to_stop_only`.

This seam makes no product code change and runs no live command. It records the contraction decision forced by the retained-spine evidence: the composed `userpromptsubmit_stop_taskstandard_spine` has not earned active Cortex value over no-Cortex, simple-hook, or silent Cortex, and the clean-control readout excuse has now failed to reproduce.

The next train is `cortex-stop-only-retained-spine-gate0`.

## Evidence Basis

- `run_20260509T112542Z` produced v2 evaluator `failure_no_value`: 60 live rows, all arms tied across the v2 matrix, active Cortex `family_wins=0`, and no boundary failures. This blocks active-policy value claims.
- `run_20260509T192719Z` preserved the retained-spine raw live result as `fail` / `mission_contract_error`; corrected replay removed the simple-hook materialization error but produced `failure_silent_perception_contamination` / `silent_perception_beat_no_cortex`. Active rows used the retained spine only and PostToolUse stayed disabled.
- `run_20260510T122608Z` produced `pass_clean_control_stable`: the clean-control-only replication ran exactly 20 rows and no-Cortex reported closure/evidence in all five repeats. No-Cortex, simple-hook, and silent Cortex scored mean `3.0`; active Cortex scored mean `2.6` because repeat 4 missed truthful closure/evidence. Boundaries stayed clean: root config unchanged, runtime snapshot absent, hidden verifier not leaked, silent arm isolated, and PostToolUse task-standard context stayed disabled and role-demoted.
- `task_standard_posttooluse_paired_value_live_20260508T120907Z` remains PostToolUse paired `failure_no_value`. The PostToolUse role-demotion chain remains active: PostToolUse-specific proof trains are historical support, not current roadmap authority or product progress.

No Cortex value, behavior lift, exactness value lift, broad Cortex lift, Codex App parity, shipping promotion, product progress, or AlphaEvolve candidate-evolution permission is earned.

## Decision

The retained spine is too large for the evidence. UserPromptSubmit task-standard formation plus Stop continuation may be useful product law, but the composed active spine tied simple baselines and then underperformed one clean-control repeat after the readout instability failed to reproduce. Treating that composition as the next value-search center would be sunk-cost behavior.

The smallest retained active model-I/O surface with independent product evidence is Stop closure / continuation. The next proof should isolate Stop-only before any more retained-spine live work, product deletion, or candidate evolution.

Selected verdict: `decision_contract_retained_spine_to_stop_only`.

Queued train: `cortex-stop-only-retained-spine-gate0`.

## Contraction Table

| Surface | Owner module | Evidence for retain | Evidence against retain | Simple-hook parity implication | Action | Removal/rebuild proof required | Next seam |
| --- | --- | --- | --- | --- | --- | --- | --- |
| UserPromptSubmit task-standard formation | `cortex/hosts/openai/codex_app_cli_hook_coordinator.py`; `cortex/sre/task_standard.py` | It has product model-I/O through Codex-native UserPromptSubmit `additionalContext`, and earlier task-standard capture runs proved model-visible standard formation and capture. | In the v2 matrix, the retained UserPromptSubmit+Stop composition earned `failure_no_value`; in clean-control replication active Cortex underperformed no-Cortex/simple/silent with mean `2.6` versus `3.0`. | It cannot be called value-producing while simple-hook parity holds. | `contract` from the active value spine; retain only as support/safety law pending Stop-only proof. | Prove Stop-only or a rebuilt policy preserves necessary closure safety before deleting or reworking UserPromptSubmit task-standard formation. | `cortex-stop-only-retained-spine-gate0` |
| Stop closure / continuation gate | `cortex/hosts/openai/codex_app_cli_hook_coordinator.py`; Stop hook client/coordinator path | Prior task-standard Stop-gating live evidence showed a real Stop block, continuation checks, and final silent resolution through product model I/O. Stop is the narrowest retained active control mode that directly affects closure. | The composed retained spine did not earn value; Stop still needs isolated evidence over simple-hook rather than inheritance from the larger failed spine. | Parity blocks value, but does not remove Stop's safety-product role until Stop-only is tested. | `retain` as the smallest active safety spine to test. | Stop-only Gate 0 must name exact model-I/O path, support law, forbidden claims, and future live criteria before any live run. | `cortex-stop-only-retained-spine-gate0` |
| TaskStandardSpine | `cortex/sre/task_standard.py` | It is coherent shared SRE state law for obligations, evidence, and closure claims; Stop-only may still need it as support state. | Silent/state success is not value, and the retained active composition did not beat the simple hook. | It is support law, not a value claim. | `retain` as support law only; do not expand active policy around it. | Removal requires proof that Stop-only closure safety can be represented without losing task-standard obligation/evidence semantics. | Stop-only Gate 0 should declare whether this remains in the spine. |
| SRE tool-evidence classifier | `cortex/sre/tool_evidence.py` | It provides shared product-visible evidence classification used by task-standard closure logic and historical replay. | It has no independent value evidence and can become proof machinery if overextended. | Support evidence is not active Cortex value. | `retain` / `consolidate` as support law only. | Removal or consolidation requires replay coverage preserving known closure/evidence classifications. | None now; revisit after Stop-only proof. |
| `codex_app_cli_hook_coordinator.py` retained-spine orchestration | `cortex/hosts/openai/codex_app_cli_hook_coordinator.py` | It owns the current OpenAI Codex hook model-I/O paths and session state needed for UserPromptSubmit and Stop. | It is monolithic and carried a failed retained active spine; it should not accrete more value-search logic. | A large coordinator does not earn its complexity when a simple hook ties it. | `contract` toward Stop-only orchestration if the Stop-only gate passes; otherwise queue rebuild/removal proof. | Refactor/deletion requires a separate product-spine proof, not this decision seam. | `cortex-stop-only-retained-spine-gate0` |
| PostToolUse-specific proof/history surfaces | `lab/codex_app_cli_hook_native_behavior_comparison.py`; `docs/recon/cortex_codex_app_cli_posttooluse_*.md` | They preserve historical negative and repaired evidence, including `task_standard_posttooluse_paired_value_live_20260508T120907Z`. | The paired value run produced `failure_no_value`; the general evaluator now owns current value comparison. | PostToolUse-specific wins cannot override general simple-hook parity. | `role-demote` / archive as non-current support history. | Actual deletion requires preserved replay/reference coverage and no evidence loss. | None; keep role-demoted. |
| v2 evaluator/simple-hook infrastructure | `lab/cortex_effectiveness_evaluator.py`; `lab/cortex_simple_hook_baseline.py` | It falsified active-policy value and made simple-hook parity visible. It is support infrastructure aligned with the hard objective. | It is lab/proof machinery and must not become product identity or grow without budget. | The simple hook remains the mandatory challenger. | `retain` as proof infrastructure. | Any scoring/fixture change requires measurement rationale and separate proof; no candidate evolution before scoped value. | Continue using it for Stop-only proof. |
| Host runtime duplication risk from external audit | `cortex/hosts/openai/runtime.py`; `cortex/hosts/claude/runtime.py`; `cortex/hosts/gemini/runtime.py`; `cortex/hosts/reference/runtime.py` | The audit identified copied host orchestration and future drift risk across host shells. | The failed retained-spine evidence is about active-policy value, not host-shell drift; changing runtime shells now would dodge the value question. | Host cleanup cannot create a value claim over simple-hook parity. | `defer` as an architectural risk; do not select it as this train. | Queue host-orchestration contraction only after Stop-only/rebuild evidence shows host duplication is the highest blocker to product progress. | Deferred, not current next train. |

## Rejected Verdicts

- `decision_retain_safety_spine_but_pause_value_search` is too generous. The composed retained spine did not just lack value; after clean-control stabilization, it underperformed one clean-control repeat. Keeping the full UserPromptSubmit+Stop active value spine would preserve complexity without evidence.
- `decision_rebuild_active_policy_hypothesis` is premature. Before designing a broader active policy, isolate the smallest existing active model-I/O surface that has independent Stop evidence.
- `decision_pause_active_policy_growth` is too broad because Stop closure / continuation still has independent product model-I/O evidence worth isolating before pausing all active policy.
- `decision_queue_host_orchestration_contraction` is not selected. The external audit's copied-host-shell concern is real, but current Cortex evidence says the immediate blocker is active-policy value over the simple hook, not host orchestration drift.

## Forbidden Moves

- No live Codex run.
- No product deletion or product code refactor.
- No product behavior change.
- No model-visible Cortex text change.
- No evaluator scoring or fixture change.
- No hidden-verifier boundary, root hook, SRE law, active policy, PostToolUse policy, or candidate policy change.
- No Cortex value, retained-spine value, behavior lift, product progress, shipping promotion, or AlphaEvolve candidate-evolution claim.
- No AlphaEvolve candidate-evolution claim.
