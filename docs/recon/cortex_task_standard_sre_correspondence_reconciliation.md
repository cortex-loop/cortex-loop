# Cortex Task-Standard SRE Correspondence Reconciliation

Surface: product / doctrine correspondence

Probe date: 2026-05-05

Verdict: task_standard_sre_correspondence_reconciled; live model-standard formation and behavior lift remain unearned.

## Standard

Cortex should treat task-standard formation as task-local executive function, not
as a Codex-only adaptor feature or lab scoring mechanism. The model-derived
standard belongs in SRE because it is active runtime goal representation and
verification-fit state. It is not Core certification truth, AUX memory, or hidden
fixture knowledge.

## What Changed

- Added SRE packet law for task-local standard formation and maintenance.
- Added `task_standard_spine` to the math-to-code map with
  `cortex/sre/task_standard.py` as the owning code home.
- Updated the model-visible output law to distinguish prior-act
  identity-continuous self-correction from prospective identity-continuous
  task-set formation.
- Kept Codex App/CLI as the current host realization of the SRE object.
- Changed no product behavior and no model-visible text.

## Correspondence Earned

- `TaskStandardSpine` is now named as a host-agnostic SRE math-to-code object.
- SRE packet law now says generic verification-shaped activity does not satisfy
  a task standard unless it aligns to the model's own standard or closure claim.
- The next live probe remains blocked on explicit task-standard text signoff and
  live Codex App/CLI context delivery evidence.

## Not Earned

- No live proof that Codex App/CLI delivers prospective task-set text.
- No live proof that a model produces the compact Work standard / Likely misses /
  Closure evidence block.
- No behavior-lift claim over raw Codex or silent-only Cortex.
- No new bio-to-code denominator row.
- No hidden verifier perception, Astro-specific rule, or PreToolUse motor
  inhibition.

## Next Decision

Queue `codex-app-cli-task-standard-live-probe` only after explicit model-visible
text signoff. The live probe should prove context delivery and standard capture,
not tune SRE law, Stop thresholds, or Cortex speech.
